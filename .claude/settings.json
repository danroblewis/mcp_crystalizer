{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "mcp__.*|Read|Grep|Glob",
        "hooks": [
          { "type": "command", "command": "cd \"${CRYSTAL_PROJECT_DIR:-$CLAUDE_PROJECT_DIR}\" && uv run python -m crystal.trace.record" }
        ]
      }
    ]
  }
}
